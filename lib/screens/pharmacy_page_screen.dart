import 'package:flutter/material.dart';

class PharmacyPageScreen extends StatelessWidget {
  const PharmacyPageScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Pharmacy page'),
      ),
    );
  }
}
